# 难题 B 重读 · 预言 B′ 复跑 · 结果 v0.2

> 2026-09-22 | 执行：`难题B重读_预言B_prime_复跑_v0.2.py`（预注册 v0.2 判据锁定）| 数据：defect_gap_pipeline_v2.json（n=304，含 f 16）
> 判定：**B″ 获判定支持（限本数据集）**——五项判据全过，稳健性两项（置换检验/LOO）均以最强形态通过
> JSON：`papers/难题B重读_预言B_prime_结果_v0.2.json`

---

## 〇、一句话

**v0.2 复跑五项判据全过：主判据（has_f 模型 ΔR²=+0.0255 过 0.02 线，系数 +0.511，p=1.56e-7）/副判据 A（MC_f 模型 ΔR²=+0.0284，p=2.83e-8）/副判据 B（带隙匹配子集 +0.562 eV，p=1.57e-2）/置换检验（10,000 次打乱标签，无一次达到真实 ΔR²，经验 p=0.0000）/LOO（逐条剔除 16 条 f 样本，系数全正 [+0.483,+0.561]，最差 p=1.34e-6）——且 v0.1 混合代理对照精确复现（ΔR²=+0.0098）。结论：**预言 B″ 成立（限本数据集）：缺陷合金带隙的 DFT 偏差，控制带隙后，含 4f 通道体系额外偏高 +0.51 eV 量级；d 电子体系无此额外项。**投影诊断（DFT 失效根源=被投影量落在投影丢弃集）获得数据的判定支持。**

## 一、判据执行表（全部）

| 判据 | 内容 | 实测 | 过否 |
|---|---|---|---|
| **主判据** | `dev~expt+has_f`：系数>0、p<0.05、ΔR²≥0.02 | 系数 **+0.511**（t=+5.37，**p=1.56e-7**）；**ΔR²=+0.0255** | ✅ |
| **副判据 A** | `dev~expt+MC_f`：同标准 | 系数 **+0.243**（**p=2.83e-8**）；**ΔR²=+0.0284** | ✅ |
| **副判据 B** | 带隙匹配子集组间差>0 且 p<0.05 | 含 f(n=16) dev=**+1.429** vs 不含(n=216) **+0.867**，diff=**+0.562**（**p=1.57e-2**） | ✅ |
| **稳健性 1** | 置换检验（10,000 次打乱 has_f）经验 p<0.05 | 真实 ΔR²=+0.0255；置换均值 +0.00096；95 分位 +0.00371；**经验 p=0.0000** | ✅✅ |
| **稳健性 2** | LOO：系数符号全程不变 | 系数 ∈ **[+0.483, +0.561] 全正**；最差 p=1.34e-6（全<0.05） | ✅✅ |
| 对照（非判据） | MC_v0.1 复算（应与 v0.1 一致） | ΔR²=**+0.0098**（与 v0.1 精确一致） | ✅ 复现一致 |

## 二、稳健性的意义（为什么 16 条样本也可立）

1. **置换检验经验 p=0.0000**：把 has_f 标签随机打乱一万次，重新拟合——**没有一次**能产生与真实相当的 ΔR²（置换分布 95 分位仅 +0.00371，真实值 +0.0255 高一个量级）。这是对小样本最公正的非参数检验，它排除了"16 条恰好凑巧"的解释；
2. **LOO 全正且最差 p=1.3e-6**：逐条剔除任一 f 样本重估——系数始终在 [+0.483,+0.561] 内，**结论不由任何单条样本驱动**（排除了"某个极端样本带飞"的解释）；
3. **两分类独立互证**：带隙匹配子集（显式控制带隙）+ 回归中的 expt 控制项（expt 系数 +0.702 显著），两个口径下 f 的额外偏差都稳定在 +0.5~+0.56 eV。

## 三、与 v0.1 的关系（纪律声明）

- **v0.1 主判定不改写**：v0.1 按当时预注册（混合代理）判"部分成立"，该记录保留（那是**代理设计**的教训，非机理的否定）；
- **v0.2 是代理按机理修正后的正当复跑**：代理重选依据="4f 通道存在性"（先证不是挑数字——La 系 f⁰ 却高偏差的反例证明"f 电子数"不能用），预注册跑前锁定；
- **对照复现一致**（M4 ΔR²=+0.0098 精确复现）→ 脚本与数据链无漂移。

## 四、结论与可用性

**结论（限本数据集，304 条合金，主要硫族化物/III-V/IV-VI 系）**：

> 缺陷合金带隙的 DFT 偏差 = 带隙依赖项（+0.702·expt）+ **f 通道项（+0.51 eV，含 4f 通道时）** + 残余；d 电子体系无额外项。

**机理**：DFT 投影（Kohn-Sham 单行列式+泛函）丢弃集=史学（多组态结构）；4f 局域通道引入最强多组态结构 → 偏差最显著。**修正方向因此不是"调泛函参数"，是"补第二读数面"**（多组态显式方法）。

**可用的三处**：
1. **八月预印本 v2 增量**（机理层补充 + f 体系专项修正方向——原 P4/P5 数据事实全部保留，本结论为其机理化与精细化）；
2. **难题靶新模式方案件**（给方向+判据+可检验预言→送有 DFT/实验条件者；否定条件已预注册在案）；
3. **投影诊断的首个数据实例**（抽象投影不可逆原理在计算材料域的实测支持——可用于原理档的证据链）。

## 五、诚实边界

1. 含 f 样本 16 条——已用置换检验+LOO 双重稳健性处置，但**样本量仍少**，外推需谨慎；
2. 代理为**组成级**（通道存在性），非电子结构级——机理的直接检验（多组态度量）未做；
3. 结论**限本数据集**（304 条、特定材料类），不外推其他材料类或方法族；
4. **d 无信号的解释未完成**：候选=(a) d 多组态弱、(b) pipeline 泛函设定对 d 已加 +U（**待核**）；
5. 本档不宣称"证明了投影诊断"——是"获得判定支持"；独立数据集复现=下一步的强检验。

## 六、下一步选项（待定调）

1. **pipeline +U 设定核查**（低成本，解释 d 无信号）；
2. **精细代理**：接 Materials Project 电子结构量（磁矩/f 占据/带隙性质）——检验力上限更高；
3. **独立数据集复现**（换材料库/换 alloy 数据源）——外部效度检验；
4. **落地**：八月预印本 v2 或难题靶方案件（按模式升级档）。

---

*难题B重读·预言B′结果 v0.2 | 2026-09-22 | 五项判据全过（主 ΔR²=+0.0255 系数+0.511 p=1.56e-7 / 副A ΔR²=+0.0284 / 副B 匹配子集+0.562eV / 置换 10000 次经验 p=0.0000 / LOO 全正最差 p=1.3e-6）+v0.1 对照精确复现；判定=B″ 获判定支持（限本数据集）：含 4f 通道体系 DFT 带隙偏差额外 +0.51 eV，d 无此额外项；机理=投影丢弃集（史学/多组态）；修正方向=补第二读数面而非调泛函；v0.1 主判定不改写（代理设计教训），v0.2 为机理修正后的正当复跑。*
