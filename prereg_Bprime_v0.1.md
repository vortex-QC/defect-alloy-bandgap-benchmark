# 难题 B 重读 · 预言 B′ 数据检验 · 预注册 v0.1

> 2026-09-22 | 判据**跑前锁定**（去幻纪律：预注册先于结果；否定如实记录，不复现=去幻事件非实验失败）
> 上游：`构造数学视角_重读旧难题靶_v0.1.md` §3.4 预言 B′
> 数据：`papers/缺陷合金带隙复现脚本/defect_gap_pipeline_v2.json`（304 条匹配；字段 formula/expt/pred/dev/endpoints）
> 性质：机理诊断的首验——**不改动八月预印本**（P4/P5 数据事实保留，本检验只测"投影诊断"是否带来额外解释力）

---

## 一、预言（精确陈述）

**B′**：缺陷合金带隙的 DFT 偏差，与"**多组态程度**"的相关性，在**控制带隙变量后**依然显著（即多组态不是带隙的代理，是独立解释项）。

**机理来源**：DFT 计算=一次投影；投影丢弃集=史学（多组态结构/自旋态竞争/近简并关联）——落在丢弃集里的体系，偏差应额外大。

## 二、变量定义（跑前锁定，不得事后调整）

| 变量 | 定义 |
|---|---|
| **dev**（因变量） | `expt − pred`（正=DFT 低估；数据源原字段） |
| **expt**（控制变量） | 实验带隙金标准（=P4 的变量） |
| **MC**（主自变量） | **加权局域电子分** = Σ_元素 (w_e × 该元素在化学式中的计量数)，其中 w_e 按元素类型：稀土/锕系（La–Lu, Ac–Lr）= **1.0**；3d 过渡金属（Sc–Zn）= **0.5**；4d/5d 过渡金属（Y–Cd, Hf–Hg）= **0.3**；主族 sp = **0** |
| MC_f（副） | 二值：化学式含稀土/锕系 |
| MC_d（副） | 二值：化学式含任一过渡金属 |

（w_e 的物理依据：4f 最局域→多组态最强；3d 次之；4d/5d 较巡游；sp 闭壳层单组态可描述。此表**跑前锁定**，不做拟合调参。）

## 三、判据（跑前锁定）

**主判据**：多元回归 `dev ~ expt + MC`（OLS）——MC 系数 **>0** 且 **p<0.05**，且相对于 `dev ~ expt` 单变量模型的 **ΔR² ≥ 0.02**。

**副判据 1**：偏相关 `corr(dev, MC | expt)` **>0** 且 p<0.05。

**副判据 2**（描述性）：含 f 组（MC_f=1, n=16）与不含 f 组的 dev 对比，**控制 expt 后**仍为正差。

**证伪条件**：MC 系数不显著（p≥0.05）或 ΔR²<0.02 或偏相关≈0 → **B′ 不成立**，投影诊断降级为待考（如实记录，不硬凑）。

**小样本如实条款**：MC_f=1 仅 16 条——若主判据由 MC_f 类样本驱动、样本量不足，须在结果中显著标注检验力限制，不宣称已证。

## 四、附：跑前已知的数据事实（探索性画像，已做）

- 304 条：含 f 元素 16 / 含 d 元素 156 / 纯 sp 133；
- dev: mean=+0.745, std=0.713, 低估占比 82.2%；
- expt: mean=1.305, std=0.838；
- 含 f 样本举例：La3InGe0.5S7(expt=2.61, dev=+2.35) / Ag0.5Eu1.75GeS4(2.14, +2.14) / DyTe1.7(0.35, −0.18)；
- **风险预判**：expt 与 MC 可能共线（含 f 体系恰好带隙偏大）——这正是必须做偏相关的原因；若共线严重（|r(expt,MC)|>0.7），主判据的分离能力受限，须如实标注。

## 五、执行

- 脚本：`papers/难题B重读_预言B_prime_检验_v0.1.py`（独立落盘，零改动原 pipeline）
- 结果：`papers/难题B重读_预言B_prime_结果_v0.1.md` + 结果 JSON

---

*预注册 v0.1 | 2026-09-22 | 判据跑前锁定：MC=加权局域电子分（w: f=1.0/3d=0.5/4d5d=0.3/sp=0）；主判据=dev~expt+MC 的 MC 系数>0 且 p<0.05 且 ΔR²≥0.02；副判据=偏相关>0 显著+含 f 组控制带隙后正差；证伪条件与 16 条小样本条款如实前置。*
