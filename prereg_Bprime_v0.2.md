# 难题 B 重读 · 预言 B′ 复跑 · 预注册 v0.2

> 2026-09-22 | 承 v0.1 结果（主判据未过=混合代理稀释；敏感性分析揭示信号全在 f 元素）
> 性质：**正当的预注册迭代**（非事后翻案）——代理按机理重选 + 补两项 v0.1 缺失的稳健性检验
> 纪律：判据跑前锁定；v0.1 主判定维持"未过"不改写；本档独立预注册，结果无论过否如实记录

---

## 一、代理重选的依据（★先证"不是挑好看的"）

**v0.1 的教训**：混合代理（f+d 同权）稀释效应——代理设计想当然，没跟随机理。

**v0.2 的机理推理**（先于任何数字）：

投影诊断的机理链是"**4f 局域通道 → 强多组态 → 落在投影丢弃集**"。判据应该落在**通道是否存在**，而不是"f 电子数"：

- **反例（v0.1 数据已验证）**：La 系（f⁰，无 f 电子）偏差却极大——La3InGe0.5S7 dev=**+2.35**、LaS1.86Se0.14 dev=**+2.50**。若用"f 电子数"作代理，La 系应被算作无多组态（预测偏差小）——**与数据相反**。
- **机理修正**：造成投影失效的是"**该体系有 4f 局域通道参与**"（无论 f 壳层是全空、半满还是全满，4f 带都在费米面附近引入局域/强关联结构）——故正确代理=**f 通道存在性**，不是 f 电子计数。

**据此跑前锁定**：
- **主代理 = has_f**（二值：化学式含 La–Lu 或 Ac–Lr）；
- **副代理 = MC_f**（f 元素在化学式中的计量数之和——捕捉"f 通道的量"）；
- **对照 = MC_v0.1**（混合加权，保留作设计教训的量化对照，不作为判据）。

（三个代理各司其职，选定理由如上——**不是按 v0.1 探索性数字高低挑选**。）

## 二、判据（跑前锁定）

**主判据**：`dev ~ expt + has_f`（OLS）——has_f 系数 **>0** 且 **p<0.05** 且 **ΔR² ≥ 0.02**（沿用 v0.1 同一条线，保持口径一致）。

**副判据 A**：`dev ~ expt + MC_f`——系数 >0 且 p<0.05 且 **ΔR² ≥ 0.02**。

**副判据 B**：带隙匹配子集组间差 >0 且 p<0.05（延续 v0.1 副判据 2）。

**稳健性三项（v0.2 新增，成败不单独定案但须全报）**：
1. **置换检验**（小样本的正确检验）：随机打乱 has_f 标签 10,000 次 → 真实 ΔR² 的位置（经验 p 值）；**要求经验 p<0.05**；
2. **留一法（LOO）**：逐条剔除 16 条 f 样本，重估 has_f 系数与 p——要求**系数符号全程不变**；
3. **对照**：MC_v0.1 同模型复算（应仍为 ΔR²≈0.0098，确认复现一致）。

**证伪条件**：主判据不过，或置换检验经验 p≥0.05，或 LOO 中出现符号翻转 → **B″ 不成立**，如实记录。

## 三、数据与执行

- 数据：`papers/缺陷合金带隙复现脚本/defect_gap_pipeline_v2.json`（n=304；含 f 16 条）；
- 脚本：`papers/难题B重读_预言B_prime_复跑_v0.2.py`（独立落盘）；
- 结果：`papers/难题B重读_预言B_prime_结果_v0.2.md` + JSON；
- 零改动原 pipeline 与 v0.1 脚本。

## 四、诚实前置

1. 本复跑**不改变 v0.1 的主判定**（v0.1 按当时预注册未过，记录保留）；
2. 含 f 样本 16 条仍少——故本轮把**置换检验**设为必过项（非参数、对小样本更公正）；
3. 若 v0.2 过线，结论表述为"**B″ 获判定支持（限本数据集）**"，不外推、不宣称普适；
4. 若 v0.2 亦不过线，则投影诊断降级为待考（诚实记录，不硬凑）。

---

*预注册 v0.2 | 2026-09-22 | 代理重选依据=机理（4f 通道存在性；La 系 f⁰ 却高偏差的反例验证"不能用 f 电子数"）；主判据=has_f 模型 ΔR²≥0.02 且系数>0 且 p<0.05（沿用 v0.1 线）；副判据 MC_f + 带隙匹配；稳健性三项（置换检验 10000 次经验 p<0.05 / LOO 符号全程不变 / MC_v0.1 对照复现）；证伪条件与诚实前置四条件锁定。*
